# Background WASM extension example

This directory is intentionally documentation-only until Extension ABI v1 is approved.
The first implementation must not invent an ABI in production code before the manifest,
permission, lifecycle, and host-capability specifications are accepted.
