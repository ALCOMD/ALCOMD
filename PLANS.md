# ExecPlan 规范

复杂任务必须使用 `docs/exec-plans/` 中的 ExecPlan。ExecPlan 是可持续更新的执行合同，不是一次性待办清单。

每份 ExecPlan 必须包含：

1. **目标**：一个可以验收的结果。
2. **非目标**：明确本里程碑不会做什么。
3. **背景与约束**：引用架构、ADR 和基线。
4. **影响范围**：允许修改的目录和禁止修改的目录。
5. **接口决策**：新增或修改的公共契约。
6. **实施步骤**：按可验证的垂直切片排列。
7. **验证命令**：可复制执行。
8. **验收标准**：客观停止条件。
9. **风险与回滚**：失败时如何恢复。
10. **进度日志**：完成项、阻塞项、偏差和后续动作。
11. **人工审批点**：必须停下等待用户决定的事项。

规则：

- 每次只执行一个里程碑。
- 发现架构冲突时停止写代码，记录到 `docs/decisions/open.md`。
- 不允许用 TODO 替代本里程碑的验收条件。
- 不允许修改验收标准来迁就现有实现。
- 里程碑完成后更新 `docs/status.md`，然后停止。
