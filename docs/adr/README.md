# Architecture Decision Records

状态值：

- `Accepted`：已接受，实施必须遵守。
- `Proposed`：待人工批准。
- `Superseded`：已被后续 ADR 替代。
- `Rejected`：不采用。

任何修改 RPC v1、数据 Schema 1、Extension API 1、权限、安装身份或迁移 Commit Point 的工作都必须先新增或更新 ADR。
